import { FormListsortPipe } from './form-listsort.pipe';

describe('FormListsortPipe', () => {
  it('create an instance', () => {
    const pipe = new FormListsortPipe();
    expect(pipe).toBeTruthy();
  });
});
