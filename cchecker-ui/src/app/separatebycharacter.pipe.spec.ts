import { SeparatebycharacterPipe } from './separatebycharacter.pipe';

describe('SeparatebycharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparatebycharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
